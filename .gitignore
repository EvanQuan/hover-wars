# Object files in Debug directory will cause merge conflicts when
# compiling. No need to store on repo when they are generated on build.
Debug

# Visual studio files are generated on build. No need to store, and may be
# different between computers.
.vs

# This file was generated on build from Blend. Should be using Visual Studio
# 2017 if on Windows, but we can ignore this anyways.
UpgradeLog.htm
